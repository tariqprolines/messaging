# Copy to .env when using Docker for frontend (browser runs on your PC, not in the container)
# Use localhost and the HOST port mapped in docker-compose (default backend: 8000)

VITE_API_URL=http://localhost:8000
VITE_API_VERSION=v1
VITE_API_TIMEOUT=30000
